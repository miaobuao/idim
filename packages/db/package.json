{
  "name": "@repo/db",
  "version": "1.0.0",
  "private": true,
  "main": "schema.ts",
  "scripts": {
    "migrate": "drizzle-kit generate:sqlite",
    "type-check": "tsc --noEmit",
    "lint": "eslint . --fix"
  },
  "dependencies": {
    "@repo/typescript-config": "workspace:*"
  }
}
